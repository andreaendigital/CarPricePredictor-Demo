name: 📚 Deploy Documentation

on:
  push:
    branches: [ main, 'SCRUM-*' ]
    paths:
      - 'docs/**'
      - 'config/mkdocs.yml'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

env:
  PYTHON_VERSION: '3.11'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    name: 📖 Build Documentation
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'

    - name: Setup environment
      run: make setup

    - name: Build documentation
      run: |
        # Copy mkdocs.yml to root and adjust paths
        cp config/mkdocs.yml ./mkdocs-temp.yml
        mkdocs build --clean --config-file mkdocs-temp.yml --site-dir site
        rm mkdocs-temp.yml

    - name: Setup Pages
      uses: actions/configure-pages@v5

    - name: Upload Pages artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: ./site

  deploy:
    name: 🚀 Deploy to GitHub Pages
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'

    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
